# Copy this file to .env.development (local) or .env.production (prod)
# Never commit real values.

APP_ENV=development
APP_USER=changeme
APP_PASS=changeme

DB_HOST=localhost
DB_PORT=5432
DB_USER=pbstuser
DB_PASS=changeme
DB_NAME=pbstdb

POKEWALLET_API_KEY=changeme
POKEWALLET_BASE_URL=https://api.pokewallet.io
